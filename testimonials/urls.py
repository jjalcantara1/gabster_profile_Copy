# urls.py
from django.urls import path
from . import views

urlpatterns = [
    # ... other URL patterns ...
    # path('add_testimonial/<str:user_to_username>/', views.add_testimonial, name='add_testimonial'),
    # path('<str:username>/', views.view_testimonials, name='view_testimonials'),
]
